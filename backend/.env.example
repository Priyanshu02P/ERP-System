# Copy this file to .env and adjust as needed.
# When running via docker-compose, DATABASE_URL is overridden in docker-compose.yml
# and points at the "db" service. Use the localhost variant only when running
# the app directly on your host machine (outside docker).

DATABASE_URL=postgresql+psycopg2://inventory:inventory@localhost:5432/inventory_db
DEBUG=true
